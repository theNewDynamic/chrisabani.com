---
var isbn10 = (isbn13) => {

	var isbn = String(isbn13).substring(3, 12)

	let sum = 0;
	let mul = 10;

	for (let i = 0; i < 9; i++) {
		sum = sum + (
			mul * parseInt(isbn[i])
		);
		mul -= 1;
	}
	
	let checkDig = 11 - (sum % 11);
	
	let z = isbn.charAt(9);

	console.log("check_digit: ", checkDig)

	{ checkDig == 10 ? "X" : checkDig == 11 ? "0" : "X" }

	let isbn10 = isbn + checkDig;
	

	return isbn10;

}
const { isbn } = Astro.props;


var isbn10 = isbn10(isbn);
// console.log("log: ", String(isbn10), typeof isbn10, isbn10.length)

// 014303877X
// 0143124951

---


<a href='https://www.amazon.com/dp/${isbn10}' >amazonyo: {isbn10}</a>

